<div class="row">
  <div class="col">
    <% if show.image.present? %>
      <%= image_tag show.image, width: 150 %>
    <% end %>
  </div>
  <div class="col col-lg-10">
    <h3><%= link_to show.title, show %></h3>
    <p><%= show.overview %></p>
    <p><%= show.seasons.count %> Seasons</p>
  </div>
</div>

<hr />
